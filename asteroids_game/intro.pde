

void intro() {
  background(0);
  click();
  buttons[0].show();
  buttons[1].show();
  buttons[2].show();
  
}

//void tactile(int x, int y) {
//  if (mouseX > 400 && mouseX < 600 && mouseY > 400 && mouseY < 500) {
//    fill(#C6C6C6);
//  }
//}
